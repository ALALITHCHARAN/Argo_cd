Ah, thanks for clarifying! If your **Argo CD dashboard is only accessible via `kubectl port-forward`**, it means:

> 🔒 You haven't exposed the `argocd-server` service outside the cluster yet.

Let’s walk through how to **properly expose Argo CD** for browser access — using **Ingress**, **NodePort**, or even a **LoadBalancer** if available.

---

# ✅ Options to Expose Argo CD Dashboard

| Method         | Works Offline?  | Secure?        | Recommended For           |
| -------------- | --------------- | -------------- | ------------------------- |
| ✅ Ingress      | ✅ Yes           | ✅ Yes          | Production/offline setups |
| ✅ NodePort     | ✅ Yes           | ⚠️ Less secure | Quick internal access     |
| ❌ LoadBalancer | ❌ (needs cloud) | ✅ Yes          | Cloud providers only      |
| ✅ Port-forward | ✅ Yes           | ✅ Dev only     | Temporary testing         |

---

# 📦 Let's Make It Accessible — Choose One Method

---

## 🔁 Option 1: Expose Argo CD with Ingress (Recommended)

> ✅ Ideal for production, and works well in **offline environments** with custom domains like `argocd.local`.

### ✅ Prerequisites

* You must have an **Ingress controller** installed (like NGINX).
* The domain (e.g., `argocd.local`) must resolve to your VM IP.

### 🔧 Ingress YAML Example

```yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd-server-ingress
  namespace: argocd
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTPS"
spec:
  ingressClassName: nginx  # Use your ingress class if different
  rules:
  - host: argocd.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: argocd-server
            port:
              number: 443
```

### 📥 Apply It:

```bash
kubectl apply -f argocd-ingress.yaml
```

### 🖥️ Update Your Local `/etc/hosts`:

On your machine:

```
sudo nano /etc/hosts
```

Add:

```
<your-vm-ip>  argocd.local
```

Then access:

```
https://argocd.local
```

---

## 🔁 Option 2: Expose via NodePort (Quickest)

Patch the `argocd-server` service:

```bash
kubectl patch svc argocd-server -n argocd -p '{"spec": {"type": "NodePort"}}'
```

Check the new port:

```bash
kubectl get svc -n argocd argocd-server
```

Example output:

```
argocd-server  NodePort  10.x.x.x  <none>  443:30900/TCP
```

Now access via:

```
https://<your-vm-ip>:30900
```

---

## 🔐 Get Admin Password

No matter how you expose it:

```bash
kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d && echo
```

---

## 🧾 Summary of Steps to Move Beyond Port-Forward

| Step                             | Command or Action                       |
| -------------------------------- | --------------------------------------- |
| ✅ Enable NodePort or Ingress     | `kubectl patch svc ...` or Ingress YAML |
| ✅ Access with browser            | `https://<vm-ip>:<nodeport>` or domain  |
| ✅ Get admin password             | `kubectl get secret ...`                |
| ✅ (Optional) update `/etc/hosts` | `sudo nano /etc/hosts`                  |

---

Would you like:

* A ready-made YAML for Ingress?
* To enable Argo CD under a subpath like `/argocd/`?
* Basic Auth or IP whitelisting?

Let me know and I’ll guide you through it.
